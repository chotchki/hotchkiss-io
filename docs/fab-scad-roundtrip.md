# fab-gui ↔ hotchkiss.io: the model round-trip

**Moved into the unified media design doc.** The round-trip contract — load, save
(`PATCH /media/<ref>`), the fab-gui-side export/upload, and the `/media/<ref>`
resource it rides — now lives in **[`docs/media-design.md`](media-design.md)**:
§10 "The 3D round-trip", §5 "The resource: `/media/<ref>`", §4 "Authorization".
This file is a pointer so existing links don't break.
